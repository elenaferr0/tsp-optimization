#include <iostream>
#include "tsp/graph.h"

using namespace std;

int main(const int argc, char *argv[]) {
    try {
    } catch (std::exception &e) {
        std::cout << "Exception: " << e.what() << std::endl;
    }
    return 0;
}
